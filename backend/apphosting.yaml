# This file configures the App Hosting backend.
# It tells App Hosting which secrets the application needs to run,
# and defines the release command to run database migrations.

runEnvironment:
  secrets:
    - secret: SECRET_KEY
    - secret: STRIPE_API_KEY
    - secret: DATABASE_URL

run:
  release:
    command: "flask db upgrade"
